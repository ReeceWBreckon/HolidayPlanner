<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fyp.p4072699.holidayplanner">

    <!-- Setup the permissions that will be used -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddLocationActivity" />
        <activity android:name=".AddStartDateActivity" />
        <activity android:name=".AddEndDateActivity" />
        <activity android:name=".ReviewHolidayActivity" />
        <activity android:name=".MyHolidaysActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".DestinationsActivity" />
        <activity android:name=".DestinationDetailActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ChangeEmailActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".NearHolidayActivity" />
        <activity android:name=".NearbyDetailsActivity" />
        <activity android:name=".NearHolidayChoiceActivity" />
        <activity android:name=".DrawerNavigation" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD4xgPlDIFiAx1kTvGBxZagsD374dmEFiQ" />
    </application>

</manifest>